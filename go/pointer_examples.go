package main

import "fmt"

// TODO - work in progress, asserts needed
func main() {
    type Node struct {
        value float64
        next *Node
    }
    var n1 Node
    n2 := new(Node)
    fmt.Printf("%v %v\n", q.i, t.i)
    type ints []int
    var p *ints = new(ints)
    fmt.Printf("%v\n", p)
}
