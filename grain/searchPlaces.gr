module SearchPlaces

from "list" include List
from "string" include String

let places = ["California", "Florida", "New York"]


let good = List.filter((s => String.startsWith("N",s)), places)
print(good)
